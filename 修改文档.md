# Transword Archive 修改文档（快速定位指南）

> 本文档用于快速理解项目结构、数据流与关键函数，减少后续改代码时的阅读与理解损耗。
> 改任何功能前，先看「核心数据流」和「关键文件速查」，再按「常见修改场景」定位到具体文件。

---

## 1. 项目概览

| 项 | 内容 |
|---|---|
| 定位 | 考研英语（一）真题学习站：真题练习 + 即点即译 + 生词/词组本 + 间隔重复背单词 |
| 技术栈 | React 18 + TypeScript + Vite + Tailwind CSS + Zustand（`persist` 存 localStorage）+ React Router（`HashRouter`）+ lucide-react 图标 |
| 测试 | Vitest（`npm test`） |
| 类型检查 | `npm run check`（`tsc -b --noEmit`） |
| 构建 | `npm run build`（`tsc -b && vite build`） |
| 本地开发 | `npm run dev`（或 `start.cmd`） |
| 部署 | Vercel / GitHub Pages，静态产物在 `dist/` |

- 数据全部存在浏览器 `localStorage`，key 为 **`transword.study`**（见 `useStudyStore.ts` 的 persist 配置）。
- 路由（`src/App.tsx`）：`/`（Home）、`/exam/:paperId`（真题）、`/vocabulary`（生词本）、`/study`（背单词）。

---

## 2. 目录结构与文件职责

```
src/
├─ App.tsx                      路由入口
├─ main.tsx                     React 挂载入口
├─ store/
│  └─ useStudyStore.ts          ★ 全局状态（Zustand + persist）。所有增删改都走这里的 action
├─ types/
│  └─ study.ts                  ★ 全部类型定义：SavedWord / SavedPhrase / WordProgress / StudySettings 等
├─ lib/
│  ├─ utils.ts                  cn() 类名合并
│  └─ useMediaQuery.ts          判断是否手机端（背单词全屏等响应式渲染用）
├─ utils/
│  ├─ study.ts                  ★ 纯函数 CRUD：upsertSavedWord / removeSavedWord / updateSavedWordMeaning /
│  │                            upsertSavedPhrase … 以及 MISSING_MEANING_PLACEHOLDER 占位文案
│  ├─ spacedRepetition.ts       ★ 间隔重复算法 + 队列构建：gradeProgress / buildDailyQueue / buildExtraQueue /
│  │                            categorizeWords / computeStudyStats / getWordStatus / markMasteredProgress
│  ├─ text.ts                   ★ 文本/分词/词形还原：normalizeWord / buildLookupCandidates /
│  │                            findClosestDictionaryWord / tokenizeParagraph / extractSentenceForWord …
│  ├─ wordLookup.ts             ★ 词库查找入口 resolveWordEntry（真题点词 & 手动加词共用）
│  ├─ doubao.ts                 ★ 跳豆包查询 openDoubao（复制文本到剪贴板 + 新标签打开 doubao.com/chat）
│  ├─ syncCodec.ts              ★ 扫码同步编解码：buildSyncFrames / parseSyncFrame / assembleSyncFrames（gzip+base64 分帧）
│  ├─ collocations.ts           ★ 词组匹配：normalizePhraseKey / resolvePhraseMeaning（读 word book/fixed_collocations.json）
│  ├─ cloze.ts                  完形填空段落切分（有对应测试 cloze.test.ts）
│  └─ *.test.ts                 单元测试
├─ data/
│  ├─ dictionary.ts             重新导出 redbookDictionary 为 dictionary
│  ├─ redbookDictionary.ts      ★ 红宝书词库：Record<string, DictionaryEntry>
│  └─ examPapers.ts             ★ 真题数据（各年份卷子）
├─ pages/
│  ├─ Home.tsx                  首页
│  ├─ ExamPage.tsx              ★ 真题作答页（点词、右键收藏、词组拖选、答题记录）
│  ├─ VocabularyPage.tsx        ★ 生词本/词组本（手动加入、改释义、分类、导出/备份、扫码同步、跳转背单词）
│  └─ StudyPage.tsx             ★ 背单词页（队列、卡片、评分、设置；手机端全屏沉浸模式）
└─ components/
   ├─ AppShell.tsx              页面外壳/导航
   ├─ WordPopover.tsx           点词释义浮层（含「查询」跳豆包、「加入生词本」按钮）
   ├─ SyncModal.tsx             ★ 扫码同步弹层：send=生成二维码轮播 / receive=摄像头连续识别导入
   ├─ InlineInteractiveText.tsx 可点击单词的正文渲染
   ├─ PassageReader.tsx         阅读篇章渲染
   ├─ QuestionPanel.tsx         选择题面板
   ├─ TextResponsePanel.tsx     翻译/写作作答面板
   ├─ StudyCard.tsx             背单词卡片（显示 word / 例句 / 释义 + 评分按钮）
   ├─ StudySummary.tsx          学习统计摘要
   └─ TodayReview.tsx           今日已学/分类单词的网格展示

word book/
├─ words.json                   生词数据（早期）
└─ fixed_collocations.json      固定搭配库（词组本自动匹配用）
```

> ★ = 改功能最常碰到的文件。

---

## 3. 核心数据流

### 3.1 单词从哪来 → 如何进生词本

```
真题点词（ExamPage）
  └─ resolveWordEntry(rawWord)      ← src/utils/wordLookup.ts（词库精确→模糊匹配）
        └─ buildSelection()          ← ExamPage 内，产出 WordSelection
              └─ store.saveWord()    ← upsertSavedWord() 合并去重 → savedWords[]

手动加入单词（VocabularyPage）
  └─ resolveWordEntry(wordDraft)     ← 自动匹配词库（可选，只是方便，非必需）
        └─ store.addWord(word, meaning, lookup)
              ├─ 已有该词 → 只更新 meaning，保留来源
              └─ 新词     → 创建 sourcePaperId/sourcePaperTitle 为空的手动单词 → savedWords[]
```

- **单词唯一键 = `word.word`（`normalizeWord` 后的小写、去首尾标点）**。所有读写（`deleteWord` / `updateWordMeaning` / `rateWord` / `markMastered` / `wordProgress`）都用这个键。
- 手动单词没有 `sourcePaperId`/`sourceContext`，因此列表里显示「手动录入」，背单词卡片里也没有「真题例句」这一步（`StudyCard` 用 `word.sourceContext` 判断是否显示例句）。

### 3.2 背单词队列怎么来的

```
StudyPage 进入 → buildDailyQueue(savedWords, wordProgress, studySettings, now)
  1) 遍历所有 savedWords：
       - 无 wordProgress 记录 → 新词（newWords）
       - nextReviewAt 已到期    → 到期复习（due，排最前）
  2) due 按到期时间升序；新词按 studySettings.order（recent/sequential/random）排序，
     并按 dailyNewCount 扣除「今日已学新词数」的额度截断
  3) 返回 [...due, ...newWords]
```

- **`buildDailyQueue` 不过滤来源**：只要在 `savedWords` 里（真题词、手动词、改过释义的词都算），都会进队列。→ 这就是「自定义加入的单词、自定义改过中文意思的单词能否进背诵列表」的关键结论：**都能进**。
- 评分 `rateWord(word, rating, revealed)` → `gradeProgress()`（简化 SM-2）更新 `wordProgress[word]`；「不认识(again)」会把该词重新插回队尾当天再见。
- 当日任务完成后「继续卷」走 `buildExtraQueue()`（新词 / 复习两类）。

### 3.3 词组本 & 豆包查询

- 真题里「拖选句子后右键」→ 直接跳豆包查询（`openDoubao`，`src/utils/doubao.ts`），不再加入词组本。
- 词组本改为仅「手动录入」（VocabularyPage → savePhrase → savedPhrases[]）。
- 单词弹层（WordPopover）新增「查询」按钮 → 跳豆包查询单个单词；「加入生词本」按钮与右键收藏仍保留。

```
手动加入词组（VocabularyPage）→ normalizePhraseKey 校验 ≥2 词 → savePhrase → savedPhrases[]
```

### 3.4 扫码同步（电脑 ↔ 手机，纯离线）

```
电脑端（VocabularyPage → 生成二维码 → SyncModal send）
  collectPayload() → buildSyncFrames()：gzip 压缩 → base64 → 按 1600 字符切成多帧
  → 每帧内容 `TWSYNC|总帧数|序号|片段`，用 qrcode.react 生成二维码自动轮播

手机端（VocabularyPage → 扫码接收 → SyncModal receive）
  getUserMedia 摄像头 → jsQR 逐帧识别 → parseSyncFrame 解析 → 收齐全部帧
  → assembleSyncFrames() 拼装 + 解压 → applyPayload() → useStudyStore.setState() 导入
```

- 帧格式见 `src/utils/syncCodec.ts`（MAGIC=`TWSYNC`，序号从 1 开始）。
- 数据量越大帧越多（几百词可能 10~30 张），电脑端自动轮播、手机端自动拼接，全程无需后端/账号。
- 摄像头需 HTTPS 或 localhost；`getUserMedia` 授权由浏览器弹窗确认。

---

## 4. 关键类型与函数速查

### SavedWord（`src/types/study.ts`）
```ts
interface SavedWord {
  word: string          // 归一化后的唯一键（小写）
  matchedWord?: string  // 词库匹配到的原形词（如 abandoned → abandon）
  meaning: string
  phonetic?: string
  partOfSpeech?: string
  sourcePaperId: string       // 手动单词为空字符串 ''
  sourcePaperTitle: string    // 手动单词为空字符串 ''
  sourceSectionId?: string
  sourcePassageId?: string
  sourceContext?: string      // 真题例句；手动单词无
  createdAt: string
}
```

### 间隔重复（`spacedRepetition.ts`）
| 函数 | 作用 |
|---|---|
| `gradeProgress(prev, word, rating, revealed, now)` | 评分推进进度（again/hard/good + 揭示层级权重） |
| `buildDailyQueue(...)` | 今日队列（到期优先 + 新词补足） |
| `buildExtraQueue(...)` | 「继续卷」额外队列 |
| `categorizeWords(...)` | 分成 unlearned / learned / mastered / consolidating |
| `getWordStatus(entry)` | 单个词状态：unlearned / learning / mastered |
| `markMasteredProgress(...)` | 手动标记已熟悉 |
| `MASTERED_INTERVAL_DAYS` | 掌握门槛（10 天） |

### 词库查找（`wordLookup.ts` / `text.ts`）
| 函数 | 作用 |
|---|---|
| `resolveWordEntry(rawWord)` | 一步到位：归一化 + 词库匹配 + 返回 entry/matchedWord |
| `normalizeWord(word)` | 小写 + 去首尾非字母（唯一键的基础） |
| `buildLookupCandidates(word)` | 词形还原候选（studies→study、running→run…） |
| `findClosestDictionaryWord(word, dict)` | 最长公共子串模糊匹配 |

### 状态库 actions（`useStudyStore.ts`）
| action | 作用 |
|---|---|
| `saveWord(selection)` | 真题点词收藏（合并去重） |
| `addWord(word, meaning, lookup?)` | **手动加词**（已存在则只改释义，否则新建无来源词） |
| `updateWordMeaning(word, meaning)` | 修改中文释义（生词本双击） |
| `savePhrase / deleteWord / deletePhrase / updatePhrase` | 词组/单词增删改 |
| `rateWord(word, rating, revealed)` | 背单词评分 |
| `markMastered(word)` | 手动标记已熟悉 |
| `setStudySettings(partial)` | 每日新词数 / 新词顺序 |
| `isSaved(word)` | 是否已收藏 |

---

## 5. 常见修改场景

### A. 给「单词」新增字段（如例句、标签）
1. `src/types/study.ts` 的 `SavedWord` 加字段（可选字段用 `?`）。
2. 写入处：`useStudyStore.ts` 的 `saveWord` / `addWord`。
3. 展示处：`VocabularyPage.tsx` 单词列表、`StudyCard.tsx` 卡片。
4. 老数据无该字段 → 记得加 `?.` 或默认值，避免 `undefined` 渲染报错。

### B. 调整背单词算法 / 队列规则
- 只改 `src/utils/spacedRepetition.ts` 即可，改完跑 `npm test`（`spacedRepetition.test.ts` 有对应用例）。

### C. 增加「手动加入」另一种内容（如固定搭配之外的笔记）
- 参照 `VocabularyPage.tsx` 里「手动加入单词」/「手动加入词组」两块 JSX + 对应 store action。
- 单词：`addWord`；词组：`savePhrase`。

### D. 让自定义内容进背诵列表（已验证结论）
- 无需改动：`buildDailyQueue` 遍历全部 `savedWords`，不区分来源。
- 若未来想「只背真题词 / 只背手动词」，在 `buildDailyQueue` 里按 `word.sourcePaperId`（空 = 手动）过滤即可。

### E. 换词库 / 加词典
- 改 `src/data/redbookDictionary.ts`（`Record<string, DictionaryEntry>`），或替换 `src/data/dictionary.ts` 的 re-export。
- 查找逻辑统一走 `resolveWordEntry`，无需改调用方。

### F. 备份 / 导入字段
- 备份导出在 `VocabularyPage.tsx` 的 `handleExportBackup`；导入在 `handleImportBackup`。
- 新增持久化字段时，要同步：① `useStudyStore.ts` 的 `partialize` ② `merge` ③ `handleImportBackup` 的类型与字段。

### G. 加新页面 / 路由
- `src/App.tsx` 加 `<Route>`；页面放在 `src/pages/`，用 `AppShell` 包裹统一外壳。

---

## 6. 测试与验证

```bash
npm run check   # TypeScript 类型检查
npm test        # Vitest 单元测试（spacedRepetition / study / cloze）
npm run build   # 生产构建
```

- 新增业务逻辑时，优先补到 `src/utils/*.test.ts`（纯函数易测）。
- **已知**：`cloze.test.ts` 里「replaces bare OCR blank numbers in order」用例当前失败，属历史遗留（与生词/背单词功能无关，改动前可先 `git stash` 复核）。
